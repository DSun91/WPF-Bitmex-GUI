<UserControl x:Class="BitmexGUI.Views.SymbolSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BitmexGUI.Views" 
             xmlns:viewmodels="clr-namespace:BitmexGUI.ViewModels" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
              d:DataContext="{d:DesignInstance Type=viewmodels:SymbolSelectionViewModel}"
             mc:Ignorable="d">
    <Border Background="AntiqueWhite" CornerRadius="15"   BorderThickness="5" Padding="10">
        <Grid> 
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" /> 
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
            <Label Content="Ticker:" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="0" Margin="0,0,0,0" VerticalAlignment="Top" FontSize="18" FontWeight="Bold"/>
            <ComboBox x:Name="Ticker" HorizontalAlignment="Left" Height="25"  Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Width="87" ItemsSource="{Binding Tickers}" SelectedValue="{Binding SelectedTicker}"/>
            <Label Content="Exchange:" HorizontalAlignment="Right"    Grid.Row="1" Grid.Column="0" VerticalAlignment="Top"  FontSize="18" FontWeight="Bold" />
            <ComboBox x:Name="ExchangeSelector" HorizontalAlignment="Left" Height="25"  Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Width="87" ItemsSource="{Binding Exchanges}" SelectedValue="{Binding SelectedExchange}"/>
            <Label Content="Time-frame:" HorizontalAlignment="Right"  Grid.Row="2" Grid.Column="0" VerticalAlignment="Top"  FontSize="18" FontWeight="Bold"/>
            <ComboBox  x:Name="Timeframe" HorizontalAlignment="Left" Height="25" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Width="87" ItemsSource="{Binding TimeFrames}" SelectedValue="{Binding SelectedTimeFrame}"/>
        </Grid>
    </Border>
</UserControl>
