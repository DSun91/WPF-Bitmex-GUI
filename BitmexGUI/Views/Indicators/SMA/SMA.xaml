<UserControl x:Class="BitmexGUI.Views.Indicators.SMA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:converters="clr-namespace:BitmexGUI.ViewModels.Utilities"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:IndicatorsToolTipConverter  x:Key="IndicatorsToolTipConverter"/>
        </ResourceDictionary>
    </UserControl.Resources> 
    <Grid>
        <ItemsControl ItemsSource="{Binding SmaSerie}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Canvas>
                        <Polyline 
                       x:Name="SMA"
                       Canvas.Top="0" 
                       Points ="{Binding Points}"
                       Stroke="{Binding Stroke}"
                       StrokeThickness="2"
                       ToolTipService.InitialShowDelay="0"
                       ToolTipService.BetweenShowDelay="0"
                       MouseMove="Polyline_MouseMove">
                            <Polyline.ToolTip>
                                <Border Background="Black" CornerRadius="2" Padding="2">
                                    <TextBlock Foreground="LightGreen" />
                                </Border>
                            </Polyline.ToolTip>
                        </Polyline> 
                    </Canvas>
                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>
     

    </Grid>
</UserControl>
